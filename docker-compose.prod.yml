version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.production
    container_name: pulseops-backend-prod
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
